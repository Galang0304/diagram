
  # Blockchain Construction Contract Flow (Copy)

  This is a code bundle for Blockchain Construction Contract Flow (Copy). The original project is available at https://www.figma.com/design/IGHm4q12dRuoKacoI3qeij/Blockchain-Construction-Contract-Flow--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  